# LongPort API Credentials
# Get your credentials from https://open.longportapp.com
LONGPORT_APP_KEY=your-app-key
LONGPORT_APP_SECRET=your-app-secret
LONGPORT_ACCESS_TOKEN=your-access-token

# Runtime Configuration
# Readonly mode: true enables readonly mode (no trading), false allows trading
LONGPORT_READONLY=true

# Server Port Configuration
# Local port mapping, service will be accessible at http://localhost:SERVER_PORT
SERVER_PORT=8000
